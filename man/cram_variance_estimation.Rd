% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cram_var_expected_loss.R
\name{cram_variance_estimation}
\alias{cram_variance_estimation}
\title{Variance Estimation for Cram Estimator}
\usage{
cram_variance_estimation(loss, batch_indices)
}
\arguments{
\item{loss}{A matrix of loss values with \eqn{N} rows (data points) and \eqn{K} columns (batches).}

\item{batch_indices}{A list where each element is a vector of indices corresponding to a batch.}
}
\value{
The estimated variance \eqn{\hat{\sigma}^2_2}.
}
\description{
This function computes the variance estimator \eqn{\hat{\sigma}^2_2}
based on the given loss matrix and batch indices.
}
\examples{
# Example usage
set.seed(123)
N <- 100  # Number of data points
K <- 10   # Number of batches

loss <- matrix(rnorm(N * K), nrow = N, ncol = K)  # Random loss values
batch_indices <- split(1:N, rep(1:K, each = N / K))

cram_variance_estimation(loss, batch_indices)
}
